mkdir /home/user/.ssh/folder

ssh-keygen
in prompt enter folder and file name of key

Change ownership of folder
sudo chown -R user:user /home/user/.ssh/folder/keyname

Add key to ssh
ssh-add /home/yaseen/.ssh/folder/keyname

update config `nano ~/.ssh/config`

```
Host github.com
  User git
  IdentityFile /path/to/your/custom_key
```

Copy key.pub to https://github.com/settings/keys


ssh-copy-id -i ~/.ssh/id_rsa.pub server@192.
